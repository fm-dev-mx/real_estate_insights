REI_DB_NAME=real_estate_db
REI_DB_USER=fm_asesor
REI_DB_PASSWORD=your_password_here
REI_DB_HOST=127.0.0.1
REI_DB_PORT=5432

# --- PROPERTY SELECTION PARAMETERS ---
# Define the criteria for selecting properties.
# These parameters will be used by the 'select_properties.py' script to filter properties from the database.

# Minimum price for properties (e.g., 2000000 for 2 million). Leave empty for no minimum.
MIN_PRICE=

# Maximum price for properties (e.g., 3000000 for 3 million). Leave empty for no maximum.
MAX_PRICE=

# Type of operation (e.g., 'venta', 'renta'). Can be a single value or a comma-separated list.
# Example: PROPERTY_OPERATION_TYPE=venta,renta
PROPERTY_OPERATION_TYPE=

# Property type (e.g., 'casa', 'departamento', 'terreno'). Can be a single value or a comma-separated list.
# Example: PROPERTY_TYPE=casa,departamento
PROPERTY_TYPE=

# Minimum number of bedrooms (integer). Leave empty for no minimum.
MIN_BEDROOMS=

# Minimum number of bathrooms (float, allows for half-baths). Leave empty for no minimum.
MIN_BATHROOMS=

# Maximum age of the property in years (integer). Leave empty for no maximum.
MAX_AGE_YEARS=

# Minimum construction area in square meters (float). Leave empty for no minimum.
MIN_CONSTRUCTION_M2=

# Minimum land area in square meters (float). Leave empty for no minimum.
MIN_LAND_M2=

# Whether the property must have parking (True/False). Leave empty for no preference.
HAS_PARKING=

# Keywords to search for in the property description (comma-separated list).
# Example: KEYWORDS_DESCRIPTION=lujo,oportunidad,vista
KEYWORDS_DESCRIPTION=
